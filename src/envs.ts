const configs = {
  plasma: {
    endpoint : "wss://plasma.dappchains.com",
    chainId : "default",
  },
  us1: {
    endpoint : "wss://test-z-us1.dappchains.com",
    chainId : "default",
  },
}

export default configs
