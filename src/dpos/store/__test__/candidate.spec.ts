describe("Validator Management", () => {

    describe("action registerCandidate", () => {
        it.skip("Checks account balance >= 1.24M LOOM")
        it.skip("Fails with feedback notification if insufficient funds")
        it.skip("Checks account allowance")
        it.skip("Asks for approval if insufficient allowance")
        it.skip("calls DPOS3.regesterCandidateAsync")
    })
})
